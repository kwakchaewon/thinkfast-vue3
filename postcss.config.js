/**
 * PostCSS 설정
 * Tailwind CSS와 Autoprefixer 플러그인 설정
 */
export default {
  plugins: {
    // Tailwind CSS 플러그인
    // tailwind.config.js의 설정을 사용하여 CSS를 처리
    tailwindcss: {},
    // Autoprefixer 플러그인
    // 브라우저 호환성을 위한 CSS vendor prefix 추가
    autoprefixer: {},
  },
}

